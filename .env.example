# Registry configuration
REGISTRY_ID=your_registry_id

# For local development (both services on one machine):
NEXT_PUBLIC_EMBED_API_URL=http://raggen-embed:8001
CORS_ORIGINS=["http://localhost:3000"]

# For production, update these values in the respective .env files:
# In raggen-web/.env:
# NEXT_PUBLIC_EMBED_API_URL=http://embed.your-domain.com:8001

# In raggen-embed/.env:
# CORS_ORIGINS=["http://web.your-domain.com"]
